name: CI

on: [push]

jobs:
  ci:
    runs-on: ${{ matrix.os }}
    env:
      CI: external
    continue-on-error: true
    strategy:
      fail-fast: false
      matrix:
        os: [macos-13-arm64, macos-13-x64]
        ruby: [3.1]
    steps:
      - uses: actions/checkout@v3
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: '${{ matrix.ruby }}'
      - run: ruby ci.rb
