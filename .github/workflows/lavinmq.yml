name: LavinMQ

on:
  workflow_dispatch:
  push:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: cloudamqp/lavinmq-action@main
      with:
        version: 2.3.0
    - run: echo $AMPQ_URL
